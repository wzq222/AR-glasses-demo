<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ar.glass">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NETWORK_SETTINGS" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        tools:ignore="ProtectedPermissions" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"
        tools:ignore="SystemPermissionTypo" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.any" />

    <application
        android:name=".core.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <!-- 主界面 -->
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTask"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 图库浏览 -->
        <activity
            android:name=".ui.GalleryActivity"
            android:screenOrientation="portrait" />

        <!-- 全屏看图 -->
        <activity
            android:name=".ui.ImageViewerActivity"
            android:screenOrientation="portrait" />

        <!-- 巡检台账 -->
        <activity
            android:name=".ui.MeterRecordsActivity"
            android:screenOrientation="portrait" />

        <!-- 手机摄像头实时检测 -->
        <activity
            android:name=".ui.LiveDetectActivity"
            android:screenOrientation="portrait" />

        <!-- 后台任务领取与SOP逐步执行 -->
        <activity
            android:name=".sop.SopActivity"
            android:screenOrientation="portrait" />

        <!-- 文件分享（导出台账 CSV/Excel） -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.ar.glass.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- BLE前台服务 -->
        <service
            android:name=".core.GlassBleService"
            android:foregroundServiceType="connectedDevice"
            android:exported="false" />

        <!-- OTA相关（SDK内置） -->
        <activity
            android:name="com.xy.ota.view.OtaUIActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name="com.xy.ota.view.ScanActivity"
            android:screenOrientation="portrait" />

        <!-- YOLO 检测自测（无需眼镜：adb broadcast 触发本机照片检测） -->
        <receiver
            android:name=".util.DetectSelfTestReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ar.glass.DETECT_SELF_TEST" />
            </intent-filter>
        </receiver>

        <!-- ADB 调试控制接口（回传链路批量测试/验证） -->
        <receiver
            android:name=".util.GlassDebugReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.ar.glass.DEBUG" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
